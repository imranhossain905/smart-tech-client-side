import React from 'react';
import useTitle from '../../hook/useTitle';

const Dashboard = () => {
    useTitle('Dashboard');
    return (
        <div>
            This is Dashboard
            
        </div>
    );
};

export default Dashboard;